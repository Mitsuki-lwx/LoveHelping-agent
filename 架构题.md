## 问题一：200 轮上下文压缩

### 三层分层架构

三层各自独立运作，通过异步管道串联。

**第一层：用户画像层**

- 存储跨轮次稳定事实（不变事实、验证过的偏好、当前话题焦点）
- 在首次全量摘要时诞生，之后每轮注入上下文
- 作用：阻止压缩导致关键事实丢失

**第二层：大文件占位替换层**

- 单条消息超过阈值的大文件直接落盘，Prompt 中仅保留结构化索引占位符
- 关键决策：在 Prompt 组装阶段就决定是否替换，保证 Prompt Cache 稳定

**第三层：全量摘要层**

- 到达警戒线时异步触发全量摘要
- 禁止工具调用，生成后原子替换旧上下文
- 回注最近的工具结果和原文，保证压缩点前后体验平滑

**可选第四层：向量数据库兜底**

- 对保真度要求极高的场景可挂载
- 全量存储 + 检索增强作为最终兜底
- 代价：检索延迟和可能的语境断裂

---

## 问题二：Agent Memory 经典框架与发展趋势

### 经典框架：四层记忆模型

| 层级      | 类型    | 作用          | 示例         |
| ------- | ----- | ----------- | ---------- |
| 短期上下文记忆 | 工作记忆  | 当前对话窗口内的信息  | 最近几轮对话     |
| 语义记忆    | 知识库   | 存储事实性知识     | 知识库、文档库    |
| 情境记忆    | 持久化历史 | 能学到东西的对话历史  | 跨会话的对话记录   |
| 程序性记忆   | 规则性记忆 | 工具调用规则、流程规则 | 系统提示词、工具定义 |

分层的本质是在**读写速度、存储成本和可解释性**之间做隔离。

### 发展趋势

- **从"怎么存"到"存哪里"**：关注点从存储格式转向存储位置策略
- **从被动到主动**：记忆系统从被动存储走向主动管理、自动演化
- **从多到精**：多元化、压缩，追求精准而非海量
- **走向平台化和可编辑**：
  - 平台化的代价是延迟增加
  - 可编辑的代价是状态爆炸
  - 未来必须是**可配置**的，而非一刀切

### 头部玩家做法

头部玩家几乎都体现了从被动存储向主动管理、自动演化的记忆机制转变：

- **外挂式记忆 vs 模型内生记忆**：两条技术路线并行
- **资本加速进场**：注重基础设施建设（云平台）
- **核心竞争点**：谁能用**最低延迟**把**最准的记忆**喂进上下文

---

## 问题三：定期复盘机制

### 五层架构

```
调度层 → 读取层 → 推理层 → 管理层 → 交付层
```

**第一层：调度层**

- 每天九点由调度器唤醒负责的 Agent
- 圈定复盘内容范围
- 过滤无价值信息

**第二层：读取层**

- 从记忆库分类拉取不同维度的信息
- 任务进度 → 时间窗口查询
- 异常 → 关键词精确匹配
- 用户画像 → 向量相似度检索
- 环境变量 → 结构化 SQL 查询
- 多路并行检索，统一汇总

**第三层：推理层**

- 按顺序执行：
  1. 事实归纳总结
  2. 对比过去的差距
  3. 提出建议和方向

**第四层：管理层**

- 写回记忆系统
- 新复盘 → 更高优先级标记
- 旧复盘 → 降低优先级

**第五层：交付层**

- 向用户输出简报：进度结论 + 建议
- 接受用户反馈
- 反馈发回第三层，作为明天的新约束

---

## 问题四：Session Busy 状态下的并发处理

### 场景

Session state 为 `busy` 时，用户新消息到达，或异步工具完成事件同时到达。

### 处理策略

**核心机制：Session 专属消息队列**

1. 每个 Session 拥有独立队列，根据 `session_id` 分发事件
2. 用户消息 → 放入 Session 专属消息队列，等当前任务完成后按顺序取出执行
3. 即时反馈 → 给用户返回即时响应
   - 同步 HTTP 接口：返回 `202 Accepted`
   - 提供轮询或 WebSocket 推送结果

**异步工具完成事件**

- 同样放入 Session 的消息队列
- 等当前任务完成后执行
- **工具回调应排在新的用户消息前面**（优先处理已有任务的收尾）

---

## 问题五：Claude Code vs OpenAI-compatible Function Calling

### Claude Code：工具即运行时

**设计哲学：** 工具不是模型的外部附件，而是 Agent 认知能力的内置模块。

**机制：** 所有工具调用经过统一的 14 步管道，强制进行权限检查、参数校验、结果审计。

| 优点                   | 缺点       |
| -------------------- | -------- |
| 安全性极强                | 过重，学习曲线陡 |
| 适合长时间运行、高自主性复杂 Agent | 与专有协议强绑定 |
| 管控力强                 | 跨平台迁移困难  |

### GLM / 豆包：OpenAI 兼容路线

**设计哲学：** 模型只负责将自然语言翻译成结构化的 `tool_calls`，执行完全交给开发者。

| 优点           | 缺点          |
| ------------ | ----------- |
| 轻量、接入快       | 工具管理职责全落开发者 |
| 跨模型迁移成本低     | 需自行实现管理逻辑   |
| 适合标准化 API 集成 | 管控力弱        |

### 结论

> 如果要构建 AI 程序员这种深度嵌入系统的高复杂 Agent，Claude Code 的强管控更适合；如果要做快速迭代或需要跨模型兼容，OpenAI 兼容范式是更好的选择。两者没有绝对优劣，是**管控力与灵活性的取舍**。
