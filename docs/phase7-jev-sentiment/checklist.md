# 用 Jev 替换情绪打分 —— 验收清单（checklist）

> 结果记录：2026-09-20。未通过项必须写明原因。

## 0. 前置
- [x] `tasks.md` / `spec.md` 写完才动代码
- [x] 密钥只走环境变量（`JEV_API_KEY`），入库前 grep 自查无明文

## 1. 实现
- [x] `JevProperties`（`app.jev.*`）+ `JevClient`（JDK `HttpClient`，**无新 maven 依赖**），超时默认 3s
- [x] `SentimentService` 两条路径：Jev 优先（启用时），失败回退原 LLM 路径
- [x] DB 语义不变：`score ∈ [-2,2]` 整数、`reason` 为文本；`SentimentLog` 字段与 API 出参形状未动
- [x] `reason` 取 Jev 返回的档位描述，**不再由模型生成句子**
- [x] 未启用时不发出任何 Jev 请求（单测断言 `calls == 0`）

## 2. 单测（`JevClientTest`，6 项，全绿）
- [x] 请求体形状：`model=jev-latest`、问题 id=`mood`、`type=score`、**5 个档位**、
      `state.user_text` 与 instructions 里的反引号引用一致、文本已去空白
- [x] 档位→分值映射：0→-2、2→0、4→+2
- [x] 档位之间的小数就近取整（3.4→3、3.6→4），不是截断
- [x] 429 / 529 / 缺 `score` 字段 / 档位越界 → **返回空、不抛异常**
- [x] 端点不可达 → 返回空、不抛异常
- [x] `enabled=false` 或密钥为空 → **零 HTTP 调用**

## 3. 真实 A/B（同批 9 例人工标注消息，两个真实应用实例，密钥真实）

| 指标 | Jev 路径 | 原 LLM 路径 |
| --- | --- | --- |
| 与人工标签完全一致 | **9/9 = 100%** | 6/9 = 66.7% |
| 落在 ±1 档内 | 9/9 = 100% | 7/9 = 77.8% |
| 延迟 p50 / max | **0.30s / 0.80s** | 1.88s / 2.79s |
| 运行期 WARN（`jev score failed`） | **0** | — |

- [x] 经 `GET /sentiment/timeline` 读到 `score` 与 `reason`（证明真落库）——
      `outputs/sentiment-jev-160958.json` / `outputs/sentiment-llm-161032.json`
- [x] **原路径的两个真缺陷被实测抓到**（不是我推测的）：
      ① `flat_daily` 的 `reason` 落库为**提示词模板原文**「（一句中文，20 字内）」——模型把模板回填进了 `[WHY]`，
      正则照收；
      ② `good_makeup` 的 `reason` 写「和好如初，心情大好」**但 `score` 是 0**——**文本与分数自相矛盾**；
      同类还有 `up_talk`（"情绪有所起色"→0）。这正是"让模型写散文再用正则抠值"的固有病。

## 4. 回归
- [x] 编译通过
- [x] 全量单测 **230/230**（基线 224 + 新增 6）
- [x] 真实 E2E **22/22**（Jev 默认关闭，即产线默认行为）

## 5. 未验证 / 已知局限（如实记录）
- [ ] **没有灰度、没有算成本**：只有 token 量与延迟的观测，未拿到官方报价；
      `app.jev.enabled` **默认 false**，要上线需显式打开并灰度
- [ ] **未测限流阈值**：9 例串行调用没触发 429/529；探针里写了退避但本轮未实测
- [ ] **代价**：丢掉了"一句话说明为什么"的自由文本 `reason`，改为存**档位描述**
      （如"低落、难过、沮丧"）。**但注意**：原路径的这段文本实测会产出模板泄漏与自相矛盾，
      所以这里丢掉的未必是净损失
- [ ] `scoreText` / `scoreConversation` 之外**没有别的调用点**被替换（本轮只换这一处）
- [ ] 未测多语言、未测超长输入（>1500 字会截断，截断行为未单独验证）
