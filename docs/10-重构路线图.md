# 10 · 重构路线图

> 原则：每个 Phase 结束时系统**可发布、可回滚**；不出现长期并行两套逻辑的中间态（RAG 收敛除外，一次性完成）。
>
> **硬性纪律**：每次修改必须端到端验证（完成定义见 09 §0，冒烟清单见 09 §7）。

## Phase 0 · 安全止血（1 周内，先行独立完成）✅ 已完成（2026-08-16）

- [x] `mcp-servers.json` 移除明文 API Key，改环境变量（07 §1）
- [x] `/tenant/token` 改为需管理凭证（07 §3-1，`AdminGuard`：X-Admin-Key 或 ADMIN 角色）
- [x] `/memory/admin/**` 加角色校验（07 §2，同上 AdminGuard）
- [x] `TerminalOperationTool`/`FileOperationTool` 从白名单摘除（07 §3-4）
- [x] CI 加 gitleaks（`.github/workflows/security.yml`，全历史扫描）

**遗留事项（人工操作）**：
1. **轮换泄漏密钥**：高德 Key（`0810a33…`）与搜索 Key（`b0236c5…`）已被提交进 git 历史（提交 `5e68470`、`855d450`），本地删除不等于撤回。需在厂商后台作废重发，新值经 `AMAP_MAPS_API_KEY` / `SEARCH_API_KEY` 环境变量注入。若仓库公开，优先级最高。
2. 本地启动前设置 `ADMIN_API_KEY` 环境变量，否则 `/tenant/token` 与 `/memory/admin/**` 一律拒绝（fail-closed）。

## Phase 1 · 地基改造（2–3 周）

- [x] **E2E 冒烟脚本化**：`scripts/e2e-smoke.sh`（15 项 PASS/FAIL，2026-08-17 首跑 15/15 全绿），后续所有变更的验证门槛
- [x] 引入 Flyway，schema.sql 迁移版本化（04 §5）：`V1__init.sql` + flyway-core/mysql，`sql.init.mode=never`，既有库 baseline 自动跳过
- [x] **检索栈收敛**：Milvus + ES → pgvector（ADR-1），删除全部 ES/Milvus 客户端/初始化器/资源；`HybridRetrievalService` 保留签名、内部改为 pgvector 门面（KnowledgeSearchTool/SkillRetriever/SkillIngestor 无缝切换）；`rag.type=pgvector`
- [x] 依赖清理：删 spring-ai-alibaba-agent-framework、Kryo、elasticsearch-java、milvus-sdk-java（ADR-8）
- [x] 删除 `TokenController`，调试统一走 `/auth/login`（ADR-13，含 SecurityConfig 放行清单清理）
- [x] 跨用户越权（IDOR）核心路径修复（2026-08-16）：`/memory/{conversationId}`、`/{conversationId}/count`、`DELETE /{conversationId}` 加归属校验，`register` 加抢注防护，ADMIN 放行；E2E 验证用户 B 读用户 A 会话返回 403、本人访问正常。Phase 1 继续审计其余按 ID 取数的路径
- [x] 修复：`/Love_app/chat/sse` 缺 `chatId` 时 500 → 400 参数校验（2026-08-16，全部 chat 端点 `@RequestParam` 必填 + `MissingServletRequestParameterException` 全局处理器；E2E 验证返回 `{"code":400,"message":"缺少必填参数: chatId"}`）
- [x] MCP 可用性解耦（第一阶段，2026-08-16）：`ToolRegistration` 改 `ObjectProvider` 可选注入，`LoveApp.toolCallbackProvider` 改 `@Autowired(required=false)`；**降级开关已验证**——`spring.ai.mcp.client.enabled=false` 时应用正常启动、纯文本聊天可用（仅本地工具）。**遗留**：MCP 开启且子进程故障时仍会启动失败（Spring AI 1.1.7 自动配置 `toolCallbackResolver→mcpToolCallbacks→mcpSyncClients` 急切初始化链，用户代码无法拦截），随 mcp-server SSE 化部署解决
- [ ] mcp-server 改 SSE 独立部署（02 §2.2，已移至 Phase 2 开头），同时解决"MCP 故障拖死启动"遗留。**前置已完成**：stdio 连接配置已从 `mcp-servers.json` 迁至 `application.yml`（JSON 文件已删除——其 env 值只原样透传、不支持任何占位符，会引发子进程循环占位符解析；密钥现经 yml Spring 占位符注入子进程）

**验收**：E2E 冒烟 15/15 全绿（2026-08-17 实测）；跨用户越权对抗测试全部拒绝（B2002）；镜像依赖显著减少（Milvus/ES SDK 移除）。

## Phase 2 · 核心管道重构（3–4 周）

- [x] **mcp-server 独立部署（Streamable HTTP，2026-08-17）**：mcp-server 独立进程（8125），主应用经 `http.connections → /mcp` 连接（**MCP 标准传输 Streamable HTTP**，SSE 已弃用）；mcp-server 补 `mcp-spring-webmvc` 依赖 + `protocol: STREAMABLE` 启用 /mcp 端点；amap（npx stdio）暂停（待 SSE/HTTP 支持恢复）。
  **遗留已消除（2026-08-18，Spring AI 升级 1.1.8 后）**：mcp-server 未启动时主应用可正常启动（1.1.8 修复 MCP client 急切初始化，E2E 验证 70s UP + 降级聊天正常）——"MCP 故障拖死启动"遗留关闭
- [x] **检索管线修复（ADR-15，2026-08-17）**：父子索引（ParentChildDocumentTransformer：53 篇 → 115 子块，子块带 parent_id/parent_text；ParentChildDocumentRetriever：子块检索、父块全文注入；旧格式向量库自动检测重建）；RAG advisor 换为 RetrievalAugmentationAdvisor。**QueryRewriter 接入（本轮完成）**：实现 QueryTransformer 挂 advisor，开关 `app.rag.query-rewrite.enabled`。**遗留**：来源引用（随 loop 重构）
- [x] **LlmGateway（ADR-7，2026-08-17 落地）**：`@Primary` FallbackChatModel——主=GoPlan（OpenAI 兼容，默认回退 DeepSeek 官方端点）、备=DeepSeek 官方（同模型双供应商容灾）；指数退避重试 3 次 + 切备 + E5000 诚实报错；token 计量 `llm.tokens{type}`（Micrometer + Prometheus 端点已启用）；单测 5/5（重试/降级/双败/禁用降级/计量）；**DashScope 聊天用途移除**（QueryRewriter/MyKeywordEnricher 改主模型，LoveAppRagCloudAdvisorConfig 删除），嵌入暂留（待嵌入调研后整体移除）。**遗留**：① 启动期文档关键词提取（MyKeywordEnricher）在主模型不可达时因"Spring AI 内置重试 × gateway 重试"嵌套放大到数分钟（已加失败降级，运行期降级演练待后续）② GoPlan 真实端点待配置（application-local.yml 的 openai 段）
- [x] **知识库扩充（P2-C）**：新增 3 篇方法论知识（非暴力沟通、依恋理论、冲突修复），与 3 篇 FAQ 互补
- [x] **记忆系统阶段 2 起步（ADR-14，2026-08-17 E2E 验证通过）**：`user_memory` + `conversation_summary` 表（Flyway V2）、异步记忆萃取管线（MemoryExtractor + MemoryExtractionScheduler，JOIN user_conversations 只萃取有归属会话）、注入协议（`<user_memory>` 结构化包裹 + 仅 ACTIVE 注入）、用户可编辑 API（`GET/PUT/DELETE /memory/facts`，越权 403 实测）、生命周期清理（候选超期清理/命中降权）、萃取脱敏（Desensitizer）
- [x] **消息表落库（Phase 2 完成项，2026-08-19 落地）**：`message` 表（Flyway V6）——含 role/content/content_hmac/prompt_version/feedback/deleted（软删）/created_at；`MessageChatMemory` 实现 Spring AI `ChatMemory`（append 全量历史 + 窗口读取，`MessageChatMemoryAdvisor` 零改动）取代 `JdbcChatMemoryRepository + MessageWindowChatMemory`；`MemoryService` 历史/计数/列表/清除全改读 `message` 表；**SPRING_AI_CHAT_MEMORY 废弃**（移除 `spring-ai-starter-model-chat-memory-repository-jdbc` 依赖、`spring.ai.chat.memory.repository.jdbc` 配置）；新增 `/memory/message/{id}/feedback`（LIKE/DISLIKE，归属校验）。**加密约束（ADR-4 汇报）**：Phase 2 为明文工作列 content，Phase 3 ADR-4 两段式迁移为 content_encrypted（V6 已预置 content_hmac 列）
- [x] **指标体系 + Grafana + 告警（Phase 2 完成项，2026-08-19 落地）**：`llm.call{provider,outcome}`（LlmGateway）、`chat.request{mode}`、`guardrail.trigger{level,rule_id}`（ChatService）落码；`ops/grafana/dashboards/lwx-ai-agent.json` + `ops/grafana/alerting/lwx-ai-agent-alerts.yml`（SLO 告警：错误率>5%、降级率>10%、L3 激增等）——部分目标指标（agent.task/chat.latency）为规划项，Grafana 面板先行、随实现补充

- [x] **用户级限流（ADR-13，2026-08-19）**：RateLimiter 租户级→用户级（`rate:user:{userId}:{date}`）代码落地并验证（第 6 次 429）。**当前配置关闭**（`enabled: false`，登录直接用，用户决定暂不启用）——代码保留，未来收费/防刷时开启。**连带修复**：① 冒烟脚本 7.2/7.3 SSE 补带 token（此前匿名共享配额）② ToolCallAgent.streamThink `getResult()` null NPE（纯 tool-call 无文本块，加 null 保护）
- [x] **图片多模态（ADR-11，2026-08-18 落地并 E2E 验证）**：`message_media` 表（Flyway V3）+ 上传接口 `POST /media/upload`（JPG/PNG/WebP、≤10MB、长边≤2048、本地 uploads 存储）；`/Love_app/chat/sse` 支持 `mediaIds`（归属校验）；**视觉模型 mimo-v2.5 经 GoPlan 端点验证可用**（手写 `VisionChatClient` 以 data URL 直发 `/chat/completions`——Spring AI 1.1.7 的 Media 编码（Resource→file://）外部端点无法识别，故绕开）。**过程中修复**：chatSse/chatSseServer 同路径路由歧义（mediaIds 请求被路由到无参版本，视觉调用从未触发——已在两个方法都加 mediaIds 转发）。**遗留**：视觉调用暂不计入 llm.tokens、亲密图像检测待做、流式改造待后续（当前同步+分块模拟流式）
- [x] **护栏升级（ADR-6，2026-08-18 落地并 E2E 验证）**：规则外置 DB（`guardrail_rule` + `guardrail_event`，Flyway V4，22 条种子规则）——L3 自伤（转介文案，含心理援助热线）/伤人/违法/PUA/注入硬阻断、L2 辱骂记录不阻断、L1 模糊记录；事件审计只存 content_hmac（07 §6）。**实现说明**：业务层显式检查（ChatService 入口 `guardrailCheck`，L3 抛 BizException 4001）——Spring AI 1.1.7 的 defaultAdvisors 链未生效（GuardrailAdvisor 未执行，随 loop 重构排查）。E2E 实测：PUA→4001 阻断、自伤→4001 转介、正常→200 无误伤、event 表有 BLOCKED/LOGGED 记录
- [x] **Agent 任务持久化（ADR-3，2026-08-19 E2E 验证通过）**：`agent_task` 表（Flyway V5，状态机 PENDING→RUNNING→SUCCESS/FAILED/CANCELLED + 心跳 + 幂等键唯一索引）；AgentTaskService 提交/流转/补偿 + AgentTaskScheduler 定时扫描（RUNNING 心跳超 10 分钟 → FAILED）；ChatService.agentChat 落库接入（**SseEmitter onCompletion 在异步线程不触发，改用 BaseAgent 完成回调钩子收尾**——E2E 抓到的框架坑）；Controller 加 idempotencyKey + 任务查询端点。**E2E 实测**：提交→SUCCESS 正常流转；提交→杀进程→重启→补偿标记 FAILED（TIMEOUT/可重提）；幂等键唯一；冒烟 15/15
- [x] **Loop 编排重构（ADR-8 落地，2026-08-20）**：**LoveApp 整个删除**（练习代码，2026-08-19）——职责由 LlmGateway/业务层护栏/RetrievalAugmentationAdvisor 拆完，ChatService 直接构建 ChatClient；**手写 ReAct 循环删除（2026-08-20）**——替换为 spring-ai-alibaba `ReactAgent`（`AgentLoopExecutor`：model=LlmGateway、saver=RedisSaver、recursionLimit=15；保 agent_task 完成回调、护栏、message 表记忆、SSE 桥接 + 🔧 工具可视化）；依赖：spring-ai-alibaba-bom 1.1.2.0 + agent-framework + graph-core + Redisson 3.24.0（netty 4.1.132/rxjava 3.1.12 离线对齐）。**遗留**：LoveAppVectorStoreConfig（内存向量库）待确认注入点后清理；stop 语义调整（取消订阅→任务由心跳补偿兜底标 FAILED，不再标 SUCCESS）

**验收**：性能测试达 NFR（09 §6）；杀进程重启 Agent 任务正确转 FAILED；降级演练通过；记忆 E2E（跨会话：用户 B 会话结束→新会话开头能引用其摘要与事实，见 09 §7.4 扩展）。

## Phase 3 · 数据与信任（2–3 周）

- [ ] 消息加密：AES-GCM 按租户派生密钥 + HMAC（ADR-4）
- [ ] 注销级联删除：DB → 向量 → Skill（ADR-5），配套定时物理清除
- [ ] 进化模块加固：审核状态前置、PII 脱敏、注入防护（06 §5）
- [ ] Golden Set + LLM-as-judge 流水线（09 §5），Prompt 版本管理（08 §2.4）
- [ ] 灰度发布机制：固定灰度桶 + 回滚指标（08 §3）
- [ ] **记忆系统阶段 1 演进（ADR-14）**：语义记忆通道——脱敏摘要向量化进 pgvector（`source=memory`），与 `user_memory` 双通道融合注入（复用 HybridRetrievalService 门面）
- [ ] **混合检索（ADR-1 更新）**：pg_trgm 关键词通道 + RRF 融合（知识库增长触发信号）＋检索质量回归集（Recall@K，09 §5.5）

**验收**：安全渗透一轮通过；Golden Set 门禁上线运行；记忆双通道检索 E2E（语义改写查询命中历史事实）。

## 立项项（⏸ 待立项，SRS §3.2 恋爱辅助核心）

- ✅ **话术三级建议（安全牌/进击牌/后撤牌，FR-CORE-01）**：已实现（2026-08-25，ADR-18 + phase4-advice）。关键词路由识别 + 系统提示词激活段 + SSE `advice` 事件 + `advice.request/activated` 指标；操控意图由 L3 兜底（V17）。
- ✅ **情绪刹车片（深夜极端情绪介入，FR-CORE-02）**：已实现（2026-08-25，挂 ADR-6 L2 梯度）。`ChatEntry.guardrailCheck` + V15 规则 + `emotion_brake.triggered` 指标。

## Phase 4+ · 差异化功能（按数据决策立项，对应 SRS §6 远期附录）

- **沙盘模拟**（真人 REALISTIC + 二次元 ANIME 双通道，ADR-12）：前置 ADR-9 与 ADR-12 定稿；**科学性与合规未证前不做**。
- **诊断三件套**：认知诊断、人格诊断报告、关系健康度仪表盘——前置 ADR-10（合规评审）定稿；**科学性与合规未证前不做**（01 §3 / 11 §5.2）。
- **用户成长等级**（1-10 级 + 毕业机制）：无 ADR，先立项（算法无数据支撑前不做）。
- **个性化推荐**：预留（ADR-16 仅列为远期触发条件，非承诺）。
- B 端运营：审核后台、配额计费（ADR-13 冻结，不在 SRS 范围）。
- **记忆系统阶段 3 演进（ADR-14）**：`relationship_profile` 关系档案（随沙盘/诊断立项）——诊断结果回填、沙盘人设引用，构成完整用户关系模型。
- RAG 阶段 4+（ADR-15）：rerank 模型（bge-reranker，召回 Top50→Top5）、增量更新（content_hash）与索引版本化（蓝绿）——数据量/变更频率触发。**2026-08-28 进度**：rerank（LLM v1）与增量更新（启动自愈）已落地；带挂着：中文混合检索关键词路（pg_trgm 实效，待分词/LIKE 方案，见 phase5-rerank 待办）、bge-reranker 模型接入、蓝绿索引。

## 里程碑总览

```
P0 安全止血 ──▶ P1 地基 ──▶ P2 核心管道 ──▶ P3 数据与信任 ──▶ P4 功能扩展
   1 周          2-3 周        3-4 周           2-3 周         按需
                 └─ 可发布 ────┴─ 可发布 ───────┴─ 对外发布门槛
```

**对外发布门槛 = P3 完成**（加密、删除权、渗透、Golden Set 全部就位）。
