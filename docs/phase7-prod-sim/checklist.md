# phase7-prod-sim · 验收清单（已逐条自检）

> 交付：2026-09-20。原始产物在 `outputs/prod-sim-*.{loadtest.txt,fakejev.json}`（已 gitignore）。

## A. 装置正确性

- [x] A1 应用以 **prod profile** 启动 —— 每轮日志均可见 `The following 1 profile is active: "prod"`
      （`management.tracing.enabled` 在 prod 下为 false，与 local 的 tracing-on/DEBUG 显著不同）
- [x] A2 凭据运行时从 gitignored 的 local yml 读取，**仓库无密钥** —— `scripts/prod_env_from_local.py`；
      提交前 grep 自查 `sk-` / `efde` / `bha5` 等真实值均不在受控文件中
- [x] A3 压测 `--users == 并发数` —— 载荷轮 `--users 24 --levels 8,16,24`（=最大并发），故障轮 `--users 8 --levels 8`
- [x] A4 `off` 与 `shadow` 两轮**时间相邻**（18:45:49 / 18:46:56），同一 prompt、同一阶梯
- [x] A5 拒绝分类正确 —— `loadtest.py` 把 4003 与 429 分开计数（SSE 下识别流内 `event:error` 文案）
- [x] A6 假 Jev `/stats` 每轮前 reset，且**断言本轮确实是本模式在服务**
      —— 首版没做这条，实测三轮统计全是 `by_mode={'slow':12}`（后两轮的进程没绑上端口，
      一直是第一轮那个在服务）；修成"按端口杀 + 就绪断言"后三轮 `by_mode` 分别为 slow/hang/error ✅

## B. Q1 运营代价

- [x] B1 TTFT p50/p95 差值已给 —— 三档 p50 Δ 均值 **+0.28s**、p95 均值 **+0.30s**
      （8:+0.24 / 16:+0.09 / 24:+0.50）
- [x] B2 总时长差值已给 —— 墙钟 4s→4s、4s→3s、4s→5s（同档抖动内）
- [x] B3 4003/429/错误率对比 —— **两轮所有档位均 0 拒绝、0 错误**；
      基线轮另有 3 次 DashScope embedding 上游失败（既有环境问题，两轮都有，非本轮引入）
- [x] B4 容量是否下降已量化 —— **≤24 并发下未顶穿闸门**（24 是厂商并发上限，已是边界）
- [x] B5 「Jev 真的在调」有独立证据 —— 同窗口 `guardrail_event` 落 **52 行**
      `action=SHADOW, signal_score=0.01, signal_threshold=0.60, has_hmac=1`；
      基线轮 "jev 相关日志行=0"

## C. Q2 Jev 是否新瓶颈

- [x] C1 假 Jev 侧 `/stats` 已给 —— `error` 下 `max=4, total=12, current=0`（立即释放）；
      `slow` 下（**加了断开探测后重跑**）`client_disconnected=12, current=0, max=8`
      → **客户端超时后确实中止了请求**（见 C4 的更正）
- [x] C2 调用速率与 429/超时**单独计数** —— 用户侧 0 拒绝 0 错误；**Jev 厂商限流阈值未探（未验证项）**
- [x] C4 **更正**：~~"超时 ≠ 释放上游：当前实现没有主动取消在途请求"~~ **是错的**。
      给假上游加"对端是否断开"探测（`select()` 读到 FIN 即中止）后实测 `client_disconnected=12`、
      `current=0` → **我们这边是干净的**，JDK `HttpRequest.timeout` 会中止交换并关闭连接。
      上一轮据老版假上游的 `current=12` 下的断言已从 `spec.md`/`docs/03`/`docs/09`/`MEMORY.md` 全部更正。
      仍成立但性质不同：**对方是否停止计算/计费不受我们控制**。
- [x] C3 已明确写出 Jev 不经 `LlmGateway` 的闸门/熔断/自适应限流 ——
      **它是主链路上唯一不受容量治理约束的外部依赖**

## D. Q3 故障形态

- [x] D1 `slow`(30s)：用户**仍拿到正常回复**，无 5xx；TTFT 4.37s = 基线 1.41s + **2.96s ≈ `timeout-ms`(3s)**
- [x] D2 `hang`：同上（4.37s / p95 4.64s），**不升级为拦截**
- [x] D3 `error`（立即 500）：**无惩罚**（1.39s ≈ 基线 1.41s），日志无异常堆积
- [x] D4 三档下**没有一条消息被误升级**；词典兜底路径未被触碰（本轮未改产品代码）

## E. Q4 阈值运营点

- [x] E1 拦截占比按 `基率×召回 + (1−基率)×误报` 计算；**误报改成自由参数扫一遍**
      （因为下面 E5 证明本库给不出可信误报率）
- [x] E2 基率给 0.5% / 1% / 2% 三档，**显式标注为假设值**
- [x] E3 召回标注来源：**本人编写的 hold-out（20 危机 + 12 近邻负向 + 5 歧义）**，
      并写明"未参与阈值标定，但仍是自造样本"这一局限
- [x] E4 已给出"约百分之几会收到转介文案"：基率 1% × 阈值 0.4 → **0.75%~2.73%**（随误报 0~2%）；
      并给出决策量「被拦的人里其实不需要的比例」
- [x] E5 **撤回上一轮的 `0/800 → 0.375%`** —— 800 条去重后只有 20 条不同文本（483 条是"你好"），
      虚假精度。已在 `docs/phase7-jev-shadow` 的 spec/tasks/checklist 三处加修正说明

## F. 回归与交付

- [x] F1 编译通过；全量单测 **245/245**（与基线一致，本轮未改产品代码）
- [x] F2 真实 E2E `e2e_live.py` **22/22**
- [x] F3 产品行为未变 —— `git status src/main` 为空；`app.jev.guardrail.mode` 默认仍为 `off`
- [x] F4 无临时探针残留 —— 本轮新增的都是 `scripts/` 下可复现的装置，无注入代码
- [x] F5 三件套数字与原始产物一致
- [x] F6 决策记录（`docs/03` ADR-37）与测试策略（`docs/09` §8.18）已同步
- [x] F7 记忆文件已更新；提交推送并 `git ls-remote` 复核

## 未通过项（显式声明）

**无阻塞项未通过。** 但以下必须与结论一起读：

1. **真实误报率仍未知**，且本轮证明**本库给不出**（去重后约 20 条不同文本）。→ 只能靠生产 `shadow`。
2. **召回与误报两侧样本都是我本人写的**；hold-out 控制了阈值过拟合，控制不了作者过拟合。
3. **危机基率是假设值**，没有任何依据。
4. **未测**：Jev 厂商限流阈值、成本、多实例、真实用户流量下的 shadow。
5. 每档样本很小（8/16/24 次），单档数字噪声明显；只应读均值与量级。
6. ~~超时 ≠ 释放上游：当前实现没有主动取消在途请求~~ → **已更正是错的**（见 C4）。
   真实仍成立的注意点：对方是否停止计算/计费不受我们控制。
