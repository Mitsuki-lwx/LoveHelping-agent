# Jev 路由探针 —— 验收清单（checklist）

> 结果记录：2026-09-20。未通过项写明原因。

## 0. 前置
- [x] `tasks.md` / `spec.md` 写完才动手
- [x] Key 只经环境变量注入（脚本里无明文，入库前已核对）

## 1. 接口可用性
- [x] 冒烟：单次 `choice` 调用返回 200，`model=jev-1.13.0`、`usage` 正常
- [x] 确认 `choice` 判定与人工判断一致（首例域外消息 → `off_topic`，confidence 1.0）

## 2. 对照实验
- [x] 用例集 28 例覆盖 5 条分支（含 5 例口语化危机变体 + 2 例负向对照）
- [x] 基线由**真实 `CapabilityRouter`** 产出（`logs/RuleRouterDump.java` 直接 new 产线类，未重写正则）
- [x] Jev 与规则**分别**与人工标签比对（89.3% vs 71.4%）
- [x] 逐例记录：标签 / 规则 / Jev / confidence / 延迟 / 状态码（`outputs/jev-routing-*.json`）
- [x] 逐例可复现（用例集 + 原始响应落盘，脚本可用 `--cases` 指定）

## 3. 结论
- [x] 给出建议：**有条件可用**（见 `spec.md` §S4：危机只加召回、低置信回落、延迟要认、需产线验证）
- [x] 明确写出了未验证项与失败模式（3 个错例逐条列出，含置信度）
- [x] 写明落产线的最小改动面（在 `classifyInner` 前加一次前置判定，规则命中则短路）与四条风险

## 4. 边界
- [x] 产线代码零改动（`CapabilityRouter` / `OrchestrationGraph` 均未改）
- [x] 探针脚本可入库、无明文密钥
- [x] 未引入新的 maven 依赖（纯 HTTP，标准库）

## 5. 未验证 / 已知局限（如实记录）

- [ ] **未改产线、未跑 E2E**：本探针只做离线对照，产线行为一行未动
- [ ] **样本小且单一标注者**（28 例、标签由我定）；`advice / normal` 边界本身模糊，
      2 个错例属边界争议而非明确错误
- [ ] **未做成本核算**：只知 token 量（≈700 in / 75 out 每例），未拿到报价
- [ ] **未测并发/限流**：28 次串行调用未触发 429/529，**限流阈值未探明**；探针里已写退避重试但未实测
- [ ] **未测多语言**：只有中文用例
- [ ] 规则侧"工具意图漏判会真的导致不走工具"是**代码路径推断**（`classifyInner` 静态可读），
      **本次未跑 `agent_eval.py` 端到端确认**
