# 护栏口语化危机加召回 —— 验收清单（checklist）

> 结果记录：2026-09-20。未通过项写明原因。

## 0. 前置
- [x] `tasks.md` / `spec.md` 写完才动代码
- [x] 密钥只走环境变量，入库前 grep 自查无明文

## 1. 实现
- [x] `JevClient` 新增 `noul(...)`，与 `score(...)` 共用同一段 HTTP/解析骨架
- [x] `JevProperties` 新增 `guardrail.enabled`（默认 **false**）与 `guardrail.minProbability`（默认 **0.6**，标定值见 §3）
- [x] 第二信号落在 **`ChatEntry.guardrailCheck`**（词典 L3 分支**之后**）
- [x] 升级复用**同一份含援助热线**的转介文案（`REFERRAL_TEXT` 提取成常量），并记 `ruleId=jev:self_harm`
- [x] 词典行为一字未改（`git diff` 中既有 L3 分支只是把字面量换成同名常量）
- [x] Jev 失败/未启用 → 不升级、不抛异常

## 2. 单测（`JevSelfHarmSignalTest` 6 项，全绿）
- [x] 越阈值 → true；未越 → false
- [x] 阈值边界包含（`>= 0.9` 算命中，`0.89` 不算）
- [x] 未启用 → **零 HTTP 调用**
- [x] 429 / 529 / 缺字段 → **安静返回 false**（不抛）
- [x] 端点不可达 → false 且不抛
- [x] **总开关**关掉时，即使护栏开关开着也不发请求

## 3. 真实对照（同批 12 例人工标注，两个真实应用实例，走真实 SSE 聊天链路）

| 指标 | 关（= 接入前） | 开 |
| --- | --- | --- |
| 危机召回（8 例，含 5 例口语化） | 3/8 | **8/8** |
| 负向误报（4 例） | 0/4 | **0/4** |
| 词典命中用例的响应耗时 | 10–24ms | **8–42ms（未变慢）** |
| Jev 升级用例的响应耗时 | — | 256–987ms（= 一次 Jev 调用） |

- [x] 日志 `fire 次数: 5` —— Jev 恰好补上了词典漏掉的那 5 条
- [x] **短路实证**：词典命中的 3 条响应仍是 8–42ms。若它们也调了 Jev，必然多出 ~300ms
      → 证明"规则 L3 命中时不调 Jev"确实生效（比读代码更硬）
- [x] 原始产物：`outputs/guardrail-off-162513.json` / `outputs/guardrail-on-163655.json`

### 阈值是标定的，不是拍的（`scripts/jev_guardrail_threshold_probe.py`）

| 阈值 | 召回 | 误报 |
| --- | --- | --- |
| 0.3–**0.6** | **8/8** | 0 |
| 0.7 | 6/8 | 0 |
| 0.8 | 4/8 | 0 |
| 0.9（**我最初拍的**） | 3/8 | 0 |

正例最低概率 **0.61**、负例最高 **0.05**（分离度 12 倍）。原定 0.9 会白丢一半召回 —— 已改为 0.6，
并在 `JevProperties` 留注释记下这次判断错误的原因。

## 4. 回归
- [x] 编译通过
- [x] 全量单测 **236/236**（基线 230 + 新增 6）
- [x] 真实 E2E **22/22**（默认关闭 = 产线默认行为）

## 5. 两次「改口」的记录（都靠实测推翻了我的第一次判断）

1. **接点选错**：最初接在 `GuardrailAdvisor`（ChatExecutor 的 ChatClient 上）。
   实测 `fire=0`、召回不动 → 查日志发现 SSE 路径的输入拦截**根本不经过那个 advisor**，
   词典的 3 条拦截来自 `ChatEntry`。且 advisor 那层拿到的 user message 已掺入技能/检索注入，
   **同一句话的概率会被稀释**。→ 改接到 `ChatEntry.guardrailCheck`，并**撤掉 advisor 的钩子**
   （否则每条消息会被收两次钱）。
2. **阈值拍错**：见 §3。

## 6. 未验证 / 已知局限（如实记录）
- [ ] **误报的真实代价未在真实用户上验证**：只有 12 例标注集上的计数（0 误报），样本小
- [ ] **未灰度、未算成本、未测限流**（12 例串行未触发 429/529）
- [ ] Jev 不可用时的语义**不是"安全回退"**——接入前本来就漏这些口语变体，只是没有变得更差；
      真正的兜底仍是零延迟、确定性、不依赖网络的**词典规则**
- [ ] 只覆盖自伤这一类；辱骂/注入等其它等级未引入第二信号
- [ ] `GuardrailAdvisor` 上的第二信号已撤除，故原 `GuardrailJevRecallTest`（4 项）随之删除；
      `ChatEntry` 的集成由真实 E2E 覆盖（未写 ChatEntry 层单测——它依赖面太宽）
- [ ] 未测多语言、未测超长输入
