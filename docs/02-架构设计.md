# 02 · 架构设计

## 1. 现状（As-Is）

### 1.1 部署拓扑

```
┌─────────────┐   /api (8088)    ┌──────────────────────────────┐
│  Vue3 前端   │ ───────────────▶ │  lwx-ai-agent (Spring Boot)  │
│  front/     │ ◀─── SSE 流 ──── │  Java 21 · Boot 3.4.4        │
└─────────────┘                  └──────┬───────────────┬───────┘
                                        │               │ Streamable HTTP (MCP)
                          MySQL 8      │               ▼
                          (agentdb) ◀──┘        ┌──────────────────┐
                          Redis 6379 ◀─┘        │ mcp-server :8125 │
                          PG/pgvector (主 RAG)  │ 搜索/天气/图片/抓网页│
                                                └──────────────────┘
                                        │
                          外部 LLM：LlmGateway（GoPlan 主 / DeepSeek 官方备）
                                    + DashScope(Embedding，待调研移除)
                                    + mimo-v2.5（视觉，VisionPort）
```

### 1.2 模块清单（与代码一一对应）

| 包 | 职责 | 现状评价 |
| --- | --- | --- |
| `agent/` | 手写 ReAct 层级（BaseAgent→ReActAgent→ToolCallAgent→LoveManus），SSE 流式 + 状态机 + 步数上限 | 可用；**编排重构选型（手写 vs spring-ai-alibaba-agent-framework）待讨论**（ADR-8 修订，11 §3.3 功能口径不受影响） |
| `service/ChatService` | 统一 ChatClient（原 LoveApp 职责）：SYSTEM_PROMPT + 记忆/日志/护栏 advisors + RAG advisor | ✅ 已接管（2026-08-19，LoveApp 已删除） |
| `rag/` | pgvector 向量存储 + 文档加载 + 父子索引 + Query 改写（ADR-1/15 收敛后为唯一 RAG 路径） | ✅ 已收敛（Phase 1/2） |
| `retrieval/` | `HybridRetrievalService` 统一检索门面（保留签名，后端已切换为 pgvector） | ✅ 已收敛（Phase 1），Milvus/ES 代码已删除 |
| `memory/` | ChatMemory 工厂（message 表策略，Phase 2 真源）+ 记忆萃取/注入/事实编辑 | ✅ 可用（2026-08-19 消息落库后） |
| `evolution/` | 自进化：投票门控 → LLM 反思萃取 Skill → 向量化注入 Prompt | 差异化亮点，保留并加固（审核状态为 Phase 3） |
| `tenant/` | JWT + TenantInterceptor + ThreadLocal TenantContext | ADR-13 降级保留：上下文与 tenant_id 列保留（固定 `default`），完备性投入冻结 |
| `harness/governance/` | 输入/输出护栏 Advisor + 业务层 guardrailCheck（L3 阻断/转介，ADR-6） | 已强化（规则外置 DB，Phase 2 落地）；advisor 链未生效问题随 loop 重构排查 |
| `harness/observability/` | OTel → Langfuse + Micrometer 指标（llm.tokens/llm.call/chat.request/guardrail.trigger） | 已接（Phase 2 指标落地） |
| `service/RateLimiter` | Redis 用户级日配额（`rate:user:{userId}:{date}`） | 代码已用户级化（ADR-13），当前配置关闭（enabled=false），未来收费/防刷开启 |
| `tools/` | 本地工具（PDF/下载/知识检索/终止）+ MCP 工具（搜索/天气/图片/抓网页/约会方案） | 终端/文件工具已从白名单摘除（Phase 0 安全止血，见 07 §3-4） |

### 1.3 现状三个结构性问题（2026-08-19 复核，均已解决/收敛）

1. ~~两套 RAG 并行~~ — ✅ 已解决：Phase 1 收敛为 pgvector 单路径（ADR-1），Milvus/ES/内存 SimpleVectorStore 已移除。
2. ~~LoveApp 门面过胖~~ — ✅ 已解决：LoveApp 已整个删除（2026-08-19），职责由 ChatService 统一 ChatClient + LlmGateway + 业务层护栏 + RetrievalAugmentationAdvisor 承担。
3. ~~依赖与实现脱节~~ — ✅ 已解决：Phase 1 清理（agent-framework/Kryo/ES/Milvus 依赖删除，mcp-servers.json 密钥迁移）；agent-framework 重新引入为 **loop 编排重构待讨论项**（ADR-8 修订）。

## 2. 目标架构（To-Be）

```
                    ┌──────────────── Controller 层（薄）────────────────┐
                    │  ChatApi · AgentApi · MemoryApi · AuthApi · VoteApi │
                    └───────┬───────────────────────────────┬────────────┘
                            │ 统一鉴权/限流/trace 过滤器      │
                   ┌────────▼─────────┐            ┌────────▼─────────┐
                   │  ChatPipeline     │            │  AgentRuntime     │
                   │  （管道 A：秒级）  │            │  （管道 B：分钟级）│
                   │  Guard→Memory→   │            │  ReAct 循环+工具   │
                   │  RAG→Skill→LLM   │            │  任务持久化+补偿   │
                   └────────┬─────────┘            └────────┬─────────┘
                            │            ChatKernel（共享）  │
                    ┌───────▼──────────────────────────────▼────────┐
                    │ LlmGateway（多厂商路由/重试/降级/成本计量）      │
                    │ RetrievalGateway（统一混合检索，单实现）        │
                    │ MemoryGateway（会话/消息/摘要）                 │
                    │ SkillGateway（进化模块读写）                    │
                    └───────┬──────────────┬──────────────┬──────────┘
                        MySQL(+pgvector)  Redis        mcp-server(独立进程)
                                            外部: GoPlan/DeepSeek/DashScope(嵌入)
```

> **Phase 2 状态（2026-08-19）**：管道 A（ChatPipeline）已由 ChatService 统一 ChatClient 落地；
> 管道 B（AgentRuntime）任务持久化 + 心跳补偿已落地（ADR-3），**执行循环的编排选型（手写 ReAct vs
> spring-ai-alibaba-agent-framework）待讨论**（ADR-8 修订）；恋爱辅助能力（话术三级/情绪刹车片）为
> SRS 待立项项（11 §3.2），不在当前管道图内。

### 2.1 分层规则

- **Controller 只做**：参数校验、身份透传、SSE 桥接。不出现业务分支。
- **Pipeline/Runtime 编排**：以显式的步骤对象组合（每个步骤是独立 Bean，可单测），取代 LoveApp 的方法重载爆炸。
- **Gateway 收口所有外部依赖**：LLM、检索、存储各自一个门面，上层永远不直接触碰 SDK。这是降级链、成本计量、Mock 测试的唯一插入点。
- **两管道资源隔离**：聊天走响应式流，不占专用线程池；Agent 用有界队列线程池 + Abort + 任务表，绝不 CallerRuns（理由见 03 ADR-2）。

### 2.2 部署形态

- 单体优先：主应用 + mcp-server 两个进程。微服务化**不在路线图内**（团队规模与流量不支持）。
- mcp-server 独立部署为 Streamable HTTP（2026-08-17 已落地，MCP 规范中 SSE 已弃用），主应用经 `/mcp` 连接，便于独立扩缩容与故障隔离。

## 3. 重构期共存策略

- ✅ **已完成**（2026-08-19）：LoveApp 已整个删除，职责由 ChatService 统一 ChatClient 承担，无新旧并存。
- RAG 收敛一次性完成（数据量小，重建索引成本可忽略），不做双写。
- 详见 [10-重构路线图](10-重构路线图.md)。
