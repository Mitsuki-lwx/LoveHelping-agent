# phase9 · 验收清单

## A. 可测性

- [ ] A1 `/admin/rag/retrieve` 支持 `rewrite=true`，且**复用产线 `QueryRewriter`**（不是另写一套）
- [ ] A2 响应回显 `effectiveQuery`（实际送去检索的 query）与 `promptVersion` —— 可归因，不靠猜
- [ ] A3 `rewrite=false` 时**一个字节都不改写**（单测用 `verify(never())` 守住）
- [ ] A4 单测覆盖：改写下用 rewritten query 检索、不改写下用原查询检索、越权路径不受影响
- [ ] A5 `retrieval_eval.py` 支持 `--rewrite`

## B. 提示词

- [ ] B1 旧版（v1）**原样保留**，作为对照组可切换（不许"顺手优化"掉对照组）
- [ ] B2 新版（v2）的约束针对**已观测到的三种失效**：概括压缩、实体丢失、注入猜测词
- [ ] B3 失效机理写进代码注释（哪一句导致哪种失效），不只写"改好了"
- [ ] B4 版本串非法时**回落 v2 并 WARN**，不静默

## C. 对照实测

- [ ] C1 三方对照（不改写 / v1 / v2）在**同一批 45 例、同一后端**上跑
- [ ] C2 报告 Recall@5 与 MRR@5，并给出与基线的差值
- [ ] C3 给出 2~3 条改写样例（定性），说明 v2 与 v1 的差别
- [ ] C4 结论按实测下：若 v2 未胜基线，**如实说"提示词不是主因"**，不为结论找台阶

## D. 交付

- [ ] D1 编译 + 全量单测通过
- [ ] D2 真实 E2E（`e2e_live.py`）通过
- [ ] D3 `enabled` 默认值**未改**
- [ ] D4 只提交本轮自己的文件（不裹挟另一条会话的未提交改动）
- [ ] D5 决策记录 / 测试策略 / 记忆已同步
- [ ] D6 明确列出未验证项
